import 'package:flutter/material.dart';

///签到日历
class SignCalendar extends StatefulWidget {

  @override
  SignCalendarState createState() => new SignCalendarState();
}

class SignCalendarState extends State<SignCalendar> {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text(''),
      ),
    );
  }

  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  void didUpdateWidget(SignCalendar oldWidget) {
    super.didUpdateWidget(oldWidget);
  }

  @override
  void didChangeDependencies() {
    super.didChangeDependencies();
  }
}
